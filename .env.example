# ============================================================
# Loan Collection Voice Agent - Environment Variables
# ============================================================
# Copy this file to .env and fill in your API keys:
#   cp .env.example .env
# ============================================================

# --- REQUIRED ---

# Deepgram API Key (for Speech-to-Text)
# Sign up at: https://console.deepgram.com/signup
# Free tier: $200 credit (no credit card needed)
DEEPGRAM_API_KEY=your_deepgram_api_key_here

# OpenAI API Key (for LLM + Text-to-Speech)
# Sign up at: https://platform.openai.com/signup
OPENAI_API_KEY=your_openai_api_key_here

# --- OPTIONAL OVERRIDES ---

# OpenAI Model (default: gpt-4o)
# OPENAI_MODEL=gpt-4o

# OpenAI TTS Voice (default: alloy)
# Options: alloy, echo, fable, onyx, nova, shimmer
# OPENAI_TTS_VOICE=alloy

# Deepgram STT Model (default: nova-3)
# DEEPGRAM_MODEL=nova-3

# Deepgram STT Language (default: multi for Hindi+English+Hinglish)
# Options: multi, hi, en, en-IN
# DEEPGRAM_LANGUAGE=multi

# --- EDGE TTS (Free, no API key needed) ---

# Edge TTS Voice (default: hi-IN-SwaraNeural for Hindi female)
# Hindi voices: hi-IN-SwaraNeural (female), hi-IN-MadhurNeural (male)
# English-India: en-IN-NeerjaNeural (female), en-IN-PrabhatNeural (male)
# EDGE_TTS_VOICE=hi-IN-SwaraNeural

# Edge TTS speaking rate (default: +0%)
# EDGE_TTS_RATE=+0%

# --- TURN SERVER (Required for cloud deployment) ---
# Without TURN, WebRTC audio won't work behind NAT/firewall.
# Free TURN: https://www.metered.ca/stun-turn or use your own.

# Single URL:   turn:global.relay.metered.ca:443
# Multiple URLs: turn:host:80, turn:host:443, turns:host:443?transport=tcp
TURN_URL=your_turn_url_here
TURN_USERNAME=your_turn_username_here
TURN_CREDENTIAL=your_turn_credential_here

# --- DEFAULT PIPELINE (when running bot.py directly) ---

# STT engine: deepgram or whisper (default: deepgram)
# PIPELINE_STT=deepgram

# TTS engine: openai or edge (default: openai)
# PIPELINE_TTS=openai

# Server port (default: 7860)
# SERVER_PORT=7860
